import 'package:flutter/material.dart';

class MisExamenesPage extends StatelessWidget {
  const MisExamenesPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
